{
  "name": "Laravel Clear Ubuntu Environment",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "features": {
    "ghcr.io/devcontainers/features/mysql:1": {
      "databaseName": "laravel",
      "databaseUser": "laravel",
      "databasePassword": "password"
    },
    "ghcr.io/devcontainers/features/php:1": {
      "installComposer": true,
      "version": "8"
    }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y software-properties-common && sudo add-apt-repository -y ppa:ondrej/php && sudo apt-get update && sudo apt-get install -y php8.2 php8.2-cli php8.2-mysql php8.2-curl php8.2-xml php8.2-mbstring php8.2-zip unzip && sudo update-alternatives --set php /usr/bin/php8.2 && composer install"
}


